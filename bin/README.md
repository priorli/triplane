# bin/

> Helper scripts for Triplane projects.

## Planned scripts (Phase 6)

- **`init.sh <project-name>`** — rename placeholders after `gh repo create my-app --template priorli/triplane`. Replaces `triplane`/`<app>` references with the new project name across `web/`, `mobile/`, package configs, Kotlin namespaces, and docs. Generates initial `.env.example` files.

This directory is empty until Phase 6 ships.
